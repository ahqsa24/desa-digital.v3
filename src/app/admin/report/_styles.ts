import styled from "styled-components";

export const NavbarButton = styled.div`
  display: flex;
  width: 100%;
  max-width: 100%;
  padding: 12px 16px;
  position: sticky;
  bottom: 0;
  justify-content: center;
  align-items: center;
  background: #FFF;
  z-index: 999;

  /* Shadow - Nav */
  box-shadow: 0px -2px 4px rgba(0, 0, 0, 0.06), 0px -4px 6px rgba(0, 0, 0, 0.10);
`;
